# Quiz Questions - Week 4

1. Why can a non-volatile stop flag loop forever?
2. What does `volatile` guarantee, and what does it not guarantee?
3. Why is `counter++` not atomic even when the field is volatile?
4. Which bug does broken DCL cause, and how does `volatile` fix publication?
5. Why are immutable snapshots safer for read-heavy concurrent config access?
