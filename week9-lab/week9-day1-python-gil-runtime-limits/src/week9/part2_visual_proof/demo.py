from __future__ import annotations


def visual_proof_steps() -> list[str]:
    return [
        "open Task Manager, htop, or py-spy",
        "run CPU-bound thread demo",
        "run CPU-bound process demo",
        "compare logical CPU utilization",
        "record command, observation, and explanation",
    ]
