from __future__ import annotations


def knight_capital_lessons() -> list[str]:
    return [
        "deployment and timing failures can become system failures",
        "old code paths and partial rollout can interact dangerously",
        "operational controls matter as much as local synchronization",
    ]
